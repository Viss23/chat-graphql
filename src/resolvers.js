/* const { gql } = require('apollo-server-koa');

const books = [
  {
    title: 'The Awakening',
    author: 'Kate Chopin',
  },
  {
    title: 'City of Glass',
    author: 'Paul Auster',
  },
];

const resolvers = gql`
 Query : {
    per: () => books
  }
`;

module.exports = resolvers;
 */
